const router = require('express').Router();
const scan = require('../controllers/scancontroller');

router.get('/scan', (req, res) => {
    console.log('GET /scan');

    //function open camera and scan barcode
    




});

module.exports = router;